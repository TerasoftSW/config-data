server:
  port: 8761
eureka:
  instance:
    hostname: registry-deploy-itwiclrlua-uc.a.run.app
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
